# Phase J — Camera bridge (for asset capture)

Source: `rails/planning/native-to-rails/epic-0-mobile-frontend-shell/MOBILE_FRONTEND_PLAN.md` lines 406–420 (path as of 2026-04-25; pre-reorg path was `rails/planning/MOBILE_FRONTEND_PLAN.md`).

---

**Goal**: Epic 5 asset entry wants to attach photos. Bridge native camera + library picker.

**Before detail-planning this phase: cross-reference the Pitfalls list below.** Items 6, 17 apply here.

**Work**:

1. Stimulus `camera_controller.js` bridges `{ source: "camera" | "library" }` to native.
2. Native picks image, uploads to Rails via existing Active Storage endpoint, returns the attachment URL/ID.
3. Web fallback: standard `<input type="file" accept="image/*" capture="environment">`.

**Verification**: take a photo inside the shell, confirm it attaches to the in-progress asset record.

**Done criteria**: asset entry flow (Epic 5 phase) can attach photos from camera and library in both shells.

---

## Pitfall cross-references (from MOBILE_FRONTEND_PLAN.md)

- **Pitfall 6** — Don't submit forms with JS that doesn't go through Turbo. Pure `fetch()` bypasses the Turbo lifecycle and the native shell won't know a new screen is being presented. Use `form.requestSubmit()` or the default submit button path.
- **Pitfall 17** — Don't assume the iOS simulator is a substitute for device QA of haptics, push, and universal links. (Camera not called out explicitly, but the same principle applies — simulator camera is a still image, not representative.)

## Phase K note

Phase K "decommission React Native app" was removed from the roadmap. RN stops being deployed once Hotwire Native reaches parity.
